#include "main.h"
#include <stdio.h>

/**
 * print_alphabet - function to print abc
 * Return: Always 0 (Success)
 */
void print_alphabet(void)
{
	char c;
	/* a function to print abc*/
	for (c = 'a'; c <= 'z'; c++)
	{
		_putchar(c);
	}
_putchar('\n');

return (0);
}

